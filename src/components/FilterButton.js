import React from 'react'

export const FilterButton = (props) => {



    return (
    
    <div>
            <button onClick={props.onFilter}>
                Washington Routes
            </button>
        </div>
    )
}
